name: flutter_top_blocked_bouncing_scroll_physics
description: Flutter scroll-physics which behaves like `BouncingScrollPhysics`, except doesn't let you to over-scroll on top.
version: 1.0.0
homepage: https://github.com/qyre-ab/flutter_top_blocked_bouncing_scroll_physics

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
